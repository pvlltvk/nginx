# Задаем размер буфера, в который будет читаться первая часть ответа от FastCGI-сервера (как правило, это небольшой заголовок ответа)
fastcgi_buffer_size    2k;

# Увеличиваем число буферов для ответов FastCGI-сервера
fastcgi_buffers    128 4k;

# Увеличиваем суммарный размер буферов, которые могут быть заняты для отправки ответа клиенту, пока ответ ещё не прочитан целиком
fastcgi_busy_buffers_size    128k;

# Увеличиваем размер данных сбрасываемых во временный файл за один раз
fastcgi_temp_file_write_size    256k;
